{
  "//": "Solution file: `tsc -b` builds the referenced packages in dependency order. Each package extends an @oxy.so/app-preset tsconfig base.",
  "files": [],
  "references": [
    { "path": "./packages/shared-types" },
    { "path": "./packages/frontend" }{{#backend}},
    { "path": "./packages/backend" }{{/backend}}
  ]
}
