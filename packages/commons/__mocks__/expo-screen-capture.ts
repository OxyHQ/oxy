/** `expo-screen-capture` stub: records which keys hold the capture guard. */
export const preventScreenCaptureAsync = jest.fn(async (_key?: string) => undefined);
export const allowScreenCaptureAsync = jest.fn(async (_key?: string) => undefined);
