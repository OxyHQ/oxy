/**
 * Re-exports Bloom's useTheme so existing imports keep working.
 * Prefer importing useTheme directly from '@oxy.so/bloom/theme' in new code.
 */
export { useTheme } from '@oxy.so/bloom/theme';
export type { Theme, ThemeColors } from '@oxy.so/bloom/theme';
